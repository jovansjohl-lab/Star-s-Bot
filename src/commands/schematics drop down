const { SlashCommandBuilder } = require('discord.js');

module.exports = {
    data: new SlashCommandBuilder()
        .setName('setupschematics')
        .setDescription('Create the schematics download menu'),

    async execute(interaction) {
        // Your embed + dropdown code goes here
        await interaction.reply({
            content: 'Schematics menu created.',
            ephemeral: true
        });
    }
};
